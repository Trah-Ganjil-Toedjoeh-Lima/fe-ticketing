version: '3'

services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile 
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development

networks:
  gmcgo_gmcgo-network:
    external: true