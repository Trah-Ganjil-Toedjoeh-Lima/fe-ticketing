version: '3'

services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile 
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development

networks:
  gmcgo_gmcgo-network:
    external: true